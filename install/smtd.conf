POSTGRES_URL=postgres:///smtd?host=/run/postgresql
PREFIX=/var/lib/smtd

# 1025 + __import__('functools').reduce(lambda x, y: x*y, map(lambda x: ord(x), list("SmtD")), 1) % (49151-1025)
USER_LISTEN_ADDRESS=[::1]:41029

# 1025 + __import__('functools').reduce(lambda x, y: x*y, map(lambda x: ord(x), list("smtd")), 1) % (49151-1025)
ADMIN_LISTEN_ADDRESS=[::1]:18379
